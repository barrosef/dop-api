# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from app.grpcapi.gen.dop.bff.v1 import runtime_pb2 as dop_dot_bff_dot_v1_dot_runtime__pb2

GRPC_GENERATED_VERSION = '1.83.1'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + ' but the generated code in dop/bff/v1/runtime_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class RuntimeServiceStub:
    """The AgentRuntime at the EDGE: running ONE conversation turn on a thread.

    The same discipline as identity.proto: identity comes from the token in
    `authorization: Bearer <token>`, the active account from the `x-account-id`
    metadata. There is no CallContext here.

    Four decisions structure this contract:

    1. **ONE turn per call, not a session.** The RPC is unary and returns one
    round's result: context → routing → model → measurement → messages →
    finding. The loop of N turns belongs to the caller, and that is precisely
    why the budget manages to interrupt between one round and the next — the
    demand PAUSES (ADR-0008 §2) instead of being cut in the middle of one.

    2. **There is no streaming here, on purpose.** Live following is the SSE that
    already exists (StreamService.WatchDemand): every published message
    becomes an event in the core (ADR-0004) and reaches the cockpit on its
    own. A second streaming path would be a second source of truth for the
    SAME timeline, and the two would diverge on the first reconnect.

    3. **The agent provider is a per-REQUEST choice.** A provider is a resource
    of category `agent` (ADR-0009), per account and per project — several
    coexist in the same process. That is why `provider` is a field of the
    request and not boot configuration. Empty = the installation's default.

    4. **Absent ≠ zeroed, here more than anywhere.** Two fields of this contract
    exist only to stop an ABSENCE of information from being read as an
    ASSERTION of zero: `cache_creation_known` and `cost_known`. See TurnUsage
    — it is the difference between "nothing was written to the cache" and
    "this provider does not count that", and between "the call was free" and
    "there is no price table". A budget fed with consolation zeroes is the
    fiction ADR-0008 exists to prevent.

    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.RunTurn = channel.unary_unary(
                '/dop.bff.v1.RuntimeService/RunTurn',
                request_serializer=dop_dot_bff_dot_v1_dot_runtime__pb2.RunTurnRequest.SerializeToString,
                response_deserializer=dop_dot_bff_dot_v1_dot_runtime__pb2.TurnOutcome.FromString,
                _registered_method=True)


class RuntimeServiceServicer:
    """The AgentRuntime at the EDGE: running ONE conversation turn on a thread.

    The same discipline as identity.proto: identity comes from the token in
    `authorization: Bearer <token>`, the active account from the `x-account-id`
    metadata. There is no CallContext here.

    Four decisions structure this contract:

    1. **ONE turn per call, not a session.** The RPC is unary and returns one
    round's result: context → routing → model → measurement → messages →
    finding. The loop of N turns belongs to the caller, and that is precisely
    why the budget manages to interrupt between one round and the next — the
    demand PAUSES (ADR-0008 §2) instead of being cut in the middle of one.

    2. **There is no streaming here, on purpose.** Live following is the SSE that
    already exists (StreamService.WatchDemand): every published message
    becomes an event in the core (ADR-0004) and reaches the cockpit on its
    own. A second streaming path would be a second source of truth for the
    SAME timeline, and the two would diverge on the first reconnect.

    3. **The agent provider is a per-REQUEST choice.** A provider is a resource
    of category `agent` (ADR-0009), per account and per project — several
    coexist in the same process. That is why `provider` is a field of the
    request and not boot configuration. Empty = the installation's default.

    4. **Absent ≠ zeroed, here more than anywhere.** Two fields of this contract
    exist only to stop an ABSENCE of information from being read as an
    ASSERTION of zero: `cache_creation_known` and `cost_known`. See TurnUsage
    — it is the difference between "nothing was written to the cache" and
    "this provider does not count that", and between "the call was free" and
    "there is no price table". A budget fed with consolation zeroes is the
    fiction ADR-0008 exists to prevent.

    """

    def RunTurn(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_RuntimeServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'RunTurn': grpc.unary_unary_rpc_method_handler(
                    servicer.RunTurn,
                    request_deserializer=dop_dot_bff_dot_v1_dot_runtime__pb2.RunTurnRequest.FromString,
                    response_serializer=dop_dot_bff_dot_v1_dot_runtime__pb2.TurnOutcome.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'dop.bff.v1.RuntimeService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('dop.bff.v1.RuntimeService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class RuntimeService:
    """The AgentRuntime at the EDGE: running ONE conversation turn on a thread.

    The same discipline as identity.proto: identity comes from the token in
    `authorization: Bearer <token>`, the active account from the `x-account-id`
    metadata. There is no CallContext here.

    Four decisions structure this contract:

    1. **ONE turn per call, not a session.** The RPC is unary and returns one
    round's result: context → routing → model → measurement → messages →
    finding. The loop of N turns belongs to the caller, and that is precisely
    why the budget manages to interrupt between one round and the next — the
    demand PAUSES (ADR-0008 §2) instead of being cut in the middle of one.

    2. **There is no streaming here, on purpose.** Live following is the SSE that
    already exists (StreamService.WatchDemand): every published message
    becomes an event in the core (ADR-0004) and reaches the cockpit on its
    own. A second streaming path would be a second source of truth for the
    SAME timeline, and the two would diverge on the first reconnect.

    3. **The agent provider is a per-REQUEST choice.** A provider is a resource
    of category `agent` (ADR-0009), per account and per project — several
    coexist in the same process. That is why `provider` is a field of the
    request and not boot configuration. Empty = the installation's default.

    4. **Absent ≠ zeroed, here more than anywhere.** Two fields of this contract
    exist only to stop an ABSENCE of information from being read as an
    ASSERTION of zero: `cache_creation_known` and `cost_known`. See TurnUsage
    — it is the difference between "nothing was written to the cache" and
    "this provider does not count that", and between "the call was free" and
    "there is no price table". A budget fed with consolation zeroes is the
    fiction ADR-0008 exists to prevent.

    """

    @staticmethod
    def RunTurn(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/dop.bff.v1.RuntimeService/RunTurn',
            dop_dot_bff_dot_v1_dot_runtime__pb2.RunTurnRequest.SerializeToString,
            dop_dot_bff_dot_v1_dot_runtime__pb2.TurnOutcome.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
